Use cases


Tasks

X * schema/migrations
  * Install
    - gem install lifetracker
    - database should live in ~/.lifetracker/
    - populate the database
    - should backup and migrate the database
  ! get test env working
  * add activity
X   - cli
    - round to nearest quarter hour
    - validations
      - category present
      - memo present
      - duplicate start time
      - overlapping activity
    - print error
    - print day after add
    - split over midnight
  * Close a day (or days?)
    - show me all days that are still open
  * Get a report of work activities
  * Run some queries for life activities
